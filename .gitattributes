# Default: let git normalize text files
* text=auto

# Windows batch scripts must keep CRLF line endings
*.bat text eol=crlf
*.cmd text eol=crlf

# Preserve vendor-signed driver packages byte-for-byte.
drivers/** binary
